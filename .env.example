# Backend
DATABASE_URL=             # MySQL connection string (mysql://user:pass@host:port/db)
JWT_ACCESS_SECRET=        # Strong random secret for access JWT signing
JWT_REFRESH_SECRET=       # Strong random secret for refresh JWT signing

# Development OTP
MOCK_OTP_CODE=123456      # Mock mobile OTP used outside production

# Admin Role
OWNER_EMAIL=              # Optional: this email receives admin role on registration
